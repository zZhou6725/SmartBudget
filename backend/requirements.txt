# 核心web框架
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# 数据库 ORM + 迁移
sqlalchemy>=2.0.35
alembic>=1.13.0
pymysql>=1.1.0

# 数据校验 pydantic v2
pydantic>=2.9.0
pydantic-settings>=2.5.0

# 缓存 redis
redis>=5.0.0

# 鉴权 jwt
pyjwt>=2.8.0
passlib[bcrypt]>=1.7.4

# 文件上传、跨域（CORSMiddleware 已内置 FastAPI）
python-multipart>=0.0.9
python-dotenv>=1.0.0

# 异步任务、LLM调用可选
httpx>=0.26.0